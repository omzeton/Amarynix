/* ********************************* */

// Remove m and t

/* Infinitive To Perfect */

if (verb == hollow) {
	// Add 'ae'
} else {
	// Geminate 3rd radical
	// Add 'ae' to it
}

/* ********************************* */
/* Infinitive To Jussive / Imperative */

if ( verb == verbClass.B || verb.length == 4 ) {
	// Reduce 3rd radical's vowel to 0 or y
} else if ( verb[0] == 'a' && verb == verbClass.A ) {
	// reduce initial a to y;
}

if ( verb == hollow ) {
	// reduce o to u
	// reduce e to i
}

/* ********************************* */
/* Infinitive To Gerund */

if ( verb == hollow ) {
	// vowel of 3rd radical 'a' to y
} else {
	// vowel of 3rd radical to y
}

if (verb.length == 3) {
	if (verb[0] !== 'a') {
		// 2nd radical give 'ae'
	}
	if (verb[2] == 'u') {
		// if 2nd radical has 'u' change it to 'o'
	}
}

if (verb[verb.length] == 'a') {
	// verbs with final 'a' receive no 'ae'
}

/* ********************************* */
/* Infinitive To Contingent */

// vowel of 3rd radical reduce to 0 or 'y'

if (verb[2] !== 'a') {
	// 2nd radical gets 'ae'
}

if ( verb == verbClass.C && verb.length == 4 ) {
	// geminate
}

if ( verb == hollow && 'with a' ) {
	// reduce it to 'y'
}

/* ********************************* */